{
  "version": "2.0.0",
  "options": {
    "cwd": "${workspaceFolder}"
  },
  "tasks": [
    {
      "label": "Start (Simulator + Watcher)",
      "type": "shell",
      "command": "./start.sh",
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "windows": {
        "command": "./start.ps1"
      }
    }
  ]
}
