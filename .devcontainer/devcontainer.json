{
  "name": "PXL Clock Dev Container",
  "image": "mcr.microsoft.com/vscode/devcontainers/dotnet:8.0",
  "customizations": {
    "vscode": {
      "extensions": ["Ionide.Ionide-fsharp", "vscode-icons-team.vscode-icons"],
      "settings": {
        "workbench.iconTheme": "vscode-icons",
        "workbench.colorTheme": "Default Dark+"
      }
    }
  }
  // No - we let the user start the watcher manually to make sure all the instructions are followed.
  // "postCreateCommand": "bash ./start.sh &",
  // "forwardPorts": [5001]
}
